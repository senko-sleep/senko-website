# Production API for the Next.js app (required on Vercel for search).
# Deploy @senko/api separately (Railway, Render, Fly, etc.) and set this to that origin, e.g.:
# NEXT_PUBLIC_API_URL=https://senko-api.example.com
NEXT_PUBLIC_API_URL=http://localhost:4000
